# ==========================================
# LOCAL DEVELOPMENT ONLY
# These values are for running locally with `npm run dev`
# For production/preview deployments, add these to Vercel Project Settings
# ==========================================

# Firebase Admin (Service Account JSON)
# Get from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# SendGrid API Key (Full Access)
# Get from: SendGrid Dashboard > Settings > API Keys
SENDGRID_API_KEY=SG.your-sendgrid-key

# Google Places API Key
# Get from: Google Cloud Console > APIs & Services > Credentials
GOOGLE_PLACES_API_KEY=your-google-places-key

# Base URL (local development)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Default Email Sender Info (for local testing)
DEFAULT_USER_NAME="Your Name"
DEFAULT_USER_COMPANY="Your Company"

# Debug API Key (optional, for testing POST /api/send locally)
DEBUG_API_KEY=local-debug-key-only
